<h1>User Posts</h1>


<% Post.all.each do |post| %>
      <h3><%= post.title %></h3>
      <% post_author = User.find(post.user_id) %>
      <h4>By <%= logged_in? ? post_author.username : "Classified" %></h4>
      <p><%= post.body %></p>
      <br>
<% end %>

